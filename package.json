{
  "name": "github-explorer",
  "version": "1.0.0",
  "description": "A CLI tool to search GitHub repos, generate summaries, and save them locally",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "search": "ts-node src/index.ts",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "ts-node": "^10.9.2",
    "@types/node": "^22.12.0",
    "vitest": "^3.0.5"
  }
}
